<ul

  aria-label="List of all tags"
  class="tag-list | cluster --gap-list --justify-center | monospace"
>
  {% for tag in tags %}
  <li>
    <a href="/tags/{{ tag }}/" data-selected="{{tag === selected}}">{{ tag }}</a>
  </li>
  {% endfor %}
</ul>
