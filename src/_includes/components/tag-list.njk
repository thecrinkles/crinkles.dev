<ul
  role="list"
  aria-label="List of all tags"
  class="hover-group-grow | cluster --gap-list --justify-center | monospace"
>
  {% for tag in tags %}
  <li>
    <a href="/tags/{{ tag }}/" class="tag" data-selected="{{tag === selected}}">{{ tag }}</a>
  </li>
  {% endfor %}
</ul>
