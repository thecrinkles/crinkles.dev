<ul
  role="list"
  aria-label="List of all tags"
  class="hover-group-grow | cluster --gap--1 --justify-center"
>
  {% for tag in tags %}
  <li>
    <a href="/tags/{{ tag }}/" class="tag" data-selected="{{tag === selected}}">{{ tag }}</a>
  </li>
  {% endfor %}
</ul>
